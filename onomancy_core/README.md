# onomancy_core

Core logic for Onomancer, the reference implementation of the [Onomancy protocol](../specs/README.md). `no_std` by default; enable the `std` feature
for standard-library integration.
