# onomancy_core

> [!WARNING]
> Alpha software. Interfaces, wire formats, and specifications change
> without notice — use at your own risk.

Core logic for Onomancer, the reference implementation of the [Onomancy protocol](../specs/README.md). `no_std` by default; enable the `std` feature for standard-library integration.
